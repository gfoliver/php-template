# Template PHP

## Instalação

* Copiar o arquivo `env.example.php` para um arquivo `env.php`;

* Inserir a url no `APP_URL` do arquivo `env.php`;

* npm install;

## Dev

* npm run watch;

## Build de produção

* npm run prod;